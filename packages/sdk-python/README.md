# earworm-sdk-python

Read-only Python helpers for Earworm session and manifest fixtures.

This package remains intentionally small in `0.2.2`. TypeScript and JSON
Schema remain canonical; the Python reader gives Oída, germ, and other local
agent integrations a stable way to inspect Earworm-compatible JSON without
reimplementing append validation.
