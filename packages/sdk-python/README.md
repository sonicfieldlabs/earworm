# earworm-sdk-python

Read-only Python helpers for Earworm session and manifest fixtures.

This package is intentionally small in `0.1.0`. TypeScript and JSON Schema remain canonical; the Python stub gives germ, hmm, and other local Python integrations a stable way to inspect Earworm-compatible JSON without reimplementing append validation.
